name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    name: Create Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Extract version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Extract changelog for version
        id: changelog
        run: |
          VERSION=${{ steps.version.outputs.VERSION }}

          # Extract changelog section for this version
          if grep -q "## \[${VERSION}\]" docs/reference/changelog.md; then
            CHANGELOG=$(awk "/## \[${VERSION}\]/{flag=1; next} /## \[/{flag=0} flag" docs/reference/changelog.md)
          else
            CHANGELOG="See [CHANGELOG](docs/reference/changelog.md) for details."
          fi

          echo "CHANGELOG<<EOF" >> $GITHUB_OUTPUT
          echo "$CHANGELOG" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: TensorCraft-HPC v${{ steps.version.outputs.VERSION }}
          body: |
            ## What's Changed

            ${{ steps.changelog.outputs.CHANGELOG }}

            ## Installation

            See [Installation Guide](docs/getting-started/installation.md) for detailed installation instructions.

            ## Quick Start

            ```bash
            cmake --preset dev
            cmake --build --preset dev --parallel 2
            ctest --preset dev --output-on-failure
            python -m pip install -e .
            ```

            ## Full Changelog

            See [Changelog](docs/reference/changelog.md) for the complete changelog.
          draft: false
          prerelease: ${{ contains(github.ref, '-alpha') || contains(github.ref, '-beta') || contains(github.ref, '-rc') }}
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-release:
    name: Build Release Artifacts
    runs-on: ubuntu-latest
    needs: release
    container:
      image: nvidia/cuda:12.8.0-devel-ubuntu22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          apt-get update
          apt-get install -y cmake ninja-build git zip

      - name: Configure
        run: |
          cmake -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_CUDA_ARCHITECTURES="70;75;80;86;89;90"

      - name: Build
        run: cmake --build build --parallel

      - name: Package headers
        run: |
          mkdir -p tensorcraft-hpc-${{ github.ref_name }}
          cp -r include tensorcraft-hpc-${{ github.ref_name }}/
          cp LICENSE README.md docs/reference/changelog.md tensorcraft-hpc-${{ github.ref_name }}/CHANGELOG.md
          zip -r tensorcraft-hpc-${{ github.ref_name }}-headers.zip tensorcraft-hpc-${{ github.ref_name }}

      - name: Upload Release Assets
        uses: softprops/action-gh-release@v2
        with:
          files: |
            tensorcraft-hpc-${{ github.ref_name }}-headers.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
