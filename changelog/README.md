# Changelog Archive

`CHANGELOG.md` at the repository root is the canonical release log.

This directory stores **process notes and historical restructuring records** that are still useful as
archive material, but are not meant to be the primary changelog surface for users landing on the
repository.
