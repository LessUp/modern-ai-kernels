# 2026-01-08

## 修复

- 修复 `src/python_ops/CMakeLists.txt` 引用不存在源文件（`tensor_bindings.cpp` / `kernel_bindings.cpp`）导致 CMake 配置失败的问题：Python 模块 `tensorcraft_ops` 现在仅编译当前仓库中存在的 `bindings.cpp`。
- 修复在未安装 CUDA Toolkit / 找不到 `nvcc` 时顶层 CMake 直接配置失败的问题：`CMakeLists.txt` 现在会在检测到可用 CUDA 编译器后才启用 CUDA，并在无 CUDA 环境下自动关闭 tests/benchmarks/python 目标以保证至少可以完成配置。
